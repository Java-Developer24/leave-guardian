

## Plan: Light Enterprise Theme Overhaul + New Pages + Layout Restructuring

This is a large overhaul covering theme change, new pages, page restructuring, and design matching to the Figma screenshots.

---

### 1. Theme: Switch to Light Enterprise Design

**File: `index.html`** — Replace font imports: swap DM Sans + Space Grotesk for **Funnel Sans** from Google Fonts.

**File: `src/index.css`** — Complete CSS variable overhaul:
- `--background: #FFFDFF` (clean white)
- `--card: #FDF8F4` (warm off-white)
- `--foreground / text: #161916`
- `--primary: #FD4E59` (coral red)
- `--accent: #FFAB28` (warm orange)
- `--muted: #FFF0DC` (soft cream)
- `--border: #E8E0D8` (warm gray)
- Remove all dark glassmorphism styles (glass-card, mesh-bg, etc.) and replace with clean light card styles with subtle warm shadows
- Update `.glass-card` to use white backgrounds with light borders and soft box-shadows
- Update `.premium-table` thead to use `#FFF0DC` cream background (matching screenshots)
- Update `.glass-input` for light theme
- Remove dark gradient definitions

**File: `tailwind.config.ts`** — Update `fontFamily` to use `Funnel Sans` instead of DM Sans / Space Grotesk.

---

### 2. Agent Dashboard: Add "Leave Summary Dashboard" Screen (Screenshot 1)

**File: `src/pages/agent/AgentSummary.tsx`** — Redesign to match screenshot 1:
- Add welcome header: "Welcome, {name}" with department subtitle and "Apply Leave" button top-right
- Show 4 KPI cards in a row: Approved Leaves, Leaves In Review, Leave Pass/Swapped, Leave Rejected (%)
- Add filter bar with "Filters Applied", "Generated By" chip, search, "Manage Column", date range picker
- Replace current table with columns: Select (checkbox), Start Date, End Date, Leave Type, Leave Category, Duration, Remark, Status, Swap, Pass, Cancel (action icons)
- Add Leave Category field ("Paid Leave") and Remark field from seed data

---

### 3. Agent Leave Page: Add Swap Leave Tab (Screenshot 2)

**File: `src/pages/agent/AgentLeave.tsx`** — Add 3-tab layout matching screenshot 2:
- Tabs: "Apply Leaves" | "Swap Leave" | "Pass the Leave"
- Current apply form goes under "Apply Leaves" tab
- "Swap Leave" tab: form with OHR/Name input, Your Date + Peer Date date pickers, Comments textarea (0/500), Submit + Cancel buttons
- Right sidebar: Calendar with color-coded legend (Available, Approved Leaves, Pending, Holidays, Unavailable), February Leave Balance card with monthly quota progress bar, My Leave Request card
- "Pass the Leave" tab: similar form for passing leave

---

### 4. Supervisor Dashboard: Match Screenshot 3

**File: `src/pages/supervisor/SupervisorHome.tsx`** — Restructure to match screenshot 3:
- Add tab navigation: "Team Management" | "Analytics"
- Welcome header: "Welcome, {name} (Supervisor)" with department
- 5 KPI cards: Pending Request, Approval Rate, Team Working Hours (e.g. 1190/1200), Team Size, Shrinkage Level
- **Pending Leave Request** section: card-based layout (not table), each card shows agent name, Date, Leave Type, Reason, Status badge, Current Shrinkage % with warning icon, "Approving leads to Shrinkage%" warning, Review Notes textarea, Approve/Reject buttons
- **Team Leave Summary** table at bottom: Agent Name, Planned Leave, Unplanned Leave, Approved Leave, Pending Leave, Working Hours (hrs) — with cream-colored header row

---

### 5. NEW: Supervisor Analytics Page (Screenshot 4)

**File: `src/pages/supervisor/SupervisorAnalytics.tsx`** — New page matching screenshot 4:
- 5 KPI cards: Forecast Accuracy (94.3%), Shrinkage% (7.8%), Team Working Hours (1190/1200), High Risk Days (3), Planned Leaves (14)
- **Planned vs Actual Attendance Trend**: Horizontal stacked bars per month (Oct-Feb), showing planned vs actual with variance labels, and "Forecasted" for future months
- **Shrinkage % Forecast**: Monthly progress bars (Jan, Feb, March) with target line at 8%, "Over Target" badge for March
- **High Risk Dates**: Card list with date, description, and severity badge (High/Moderate)
- **Department Risk Analysis**: Progress bars per department with shrinkage %, risk level badges
- **System Recommendations**: Action cards with "Accept recommendation" / "Override with reason" / "View Details" buttons

**File: `src/app/router.tsx`** — Add route `/supervisor/analytics`

**File: `src/components/layout/AppShell.tsx`** — Add "Analytics" nav item to supervisor section

---

### 6. Admin: Combine Leave Window + Shrinkage into Single Tabbed Page (Screenshot 5 reference)

**File: `src/pages/admin/config/AdminConfig.tsx`** — New combined page:
- Tab switcher: "Leave Window" | "Shrinkage Rules"
- Each tab renders the existing content from AdminLeaveWindow and AdminShrinkage respectively
- Clean light card design matching the enterprise theme

**File: `src/app/router.tsx`** — Replace separate routes with single `/admin/config` route

**File: `src/components/layout/AppShell.tsx`** — Combine "Leave Window" + "Shrinkage Rules" nav items into single "Configuration" item pointing to `/admin/config`

---

### 7. Admin Analytics: Match Screenshot 5/6 Style

**File: `src/pages/admin/AdminAnalytics.tsx`** — Add matching sections:
- Same KPI row as supervisor analytics (Forecast Accuracy, Shrinkage%, Team Working Hours, High Risk Days, Planned Leaves)
- Planned vs Actual Attendance Trend (horizontal bars)
- Shrinkage % Forecast with target bars
- High Risk Dates cards
- Department Risk Analysis bars
- System Recommendations cards
- Keep existing department breakdown table and charts

---

### Summary of Files to Create/Modify

| File | Action |
|------|--------|
| `index.html` | Modify (font) |
| `src/index.css` | Major rewrite (light theme) |
| `tailwind.config.ts` | Modify (font family) |
| `src/pages/agent/AgentSummary.tsx` | Redesign (screenshot 1) |
| `src/pages/agent/AgentLeave.tsx` | Add tabs (screenshot 2) |
| `src/pages/supervisor/SupervisorHome.tsx` | Redesign with tabs (screenshot 3) |
| `src/pages/supervisor/SupervisorAnalytics.tsx` | **New file** (screenshot 4) |
| `src/pages/admin/config/AdminConfig.tsx` | **New file** (combined tabs) |
| `src/pages/admin/AdminAnalytics.tsx` | Enhance (screenshots 5/6) |
| `src/app/router.tsx` | Add/modify routes |
| `src/components/layout/AppShell.tsx` | Update nav items |
| `src/components/kpis/KpiCard.tsx` | Update for light theme |
| `src/components/SectionHeader.tsx` | Update for light theme |

